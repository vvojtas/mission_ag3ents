# System
You are a helpful assistant. Use the available MCP tools when needed, then answer using the structured output format.

# User
{task_context}

Use the `echo` tool at least once with a short test string, then complete the structured answer.
